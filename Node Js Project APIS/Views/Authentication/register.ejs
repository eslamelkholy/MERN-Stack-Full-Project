<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Registration Page</title>
    <link href="css/bootstrap.css" rel="stylesheet" />
    <link href="css/font-awesome.css" rel="stylesheet"/>
    <style>
        *{
            padding: 0;
            margin: 0;
        }
        .container{
            margin-top: 50px;
        }
        h1{
            color: lightseagreen;
            margin-bottom: 50px;
        }
        span{
            float: right;
            width: 200px;
            text-align: center;
            border-radius: 10%;

        }
    </style>
</head>
<body>
      <div class="container">
        <h1 align="Center">Speakers Register Page</h1>
        <form class="col-6 offset-3" method="POST" action="/register">
            <div class="form-group">
                <label>Full Name</label>
                <input type="text" class="form-control" name="fullName" />
                <% if(locals.message && locals.message.fullName){ %>
                <span class="bg-danger text-white">Invalid fullName</span>
                <% }%>
            </div>
            <div class="form-group">
                <label>UserName</label>
                <input type="text" class="form-control" name="username" />
                <% if(locals.message && locals.message.username){ %>
                <span class="bg-danger text-white">Invalid Username</span>
                <% } %>
            </div>
             <div class="form-group">
                 <label>UserPass</label>
                 <input type="password" class="form-control" name="password" />
                 <% if(locals.message && locals.message.password){ %>
                <span class="bg-danger text-white">Invalid Password</span>
                <% } %>
             </div>
             <div class="form-group">
                <label>Email</label>
                <input type="text" class="form-control" name="email" />
                <% if(locals.message && locals.message.email){ %>
                <span class="bg-danger text-white">Invalid Email</span>
                <% } %>
            </div>
             <div class="form-group">
                <label>Address</label>
                <input type="text" class="form-control" name="address" />
                <% if(locals.message && locals.message.address){ %>
                <span class="bg-danger text-white">Invalid Address</span>
                <% } %>
            </div>
            <input type="submit" value="Register" class="btn btn-primary">
         </form>
      </div>
    <script src="js/JQuery-3.3.1.min.js"></script>
    <script src="js/popper.js"></script>
    <script src="js/bootstrap.js"></script>
</body>
</html>
